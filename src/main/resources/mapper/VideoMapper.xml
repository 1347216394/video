<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qf.dao.VideoMapper">
    <select id="findAll" resultType="com.qf.dto.VideoDto">
        select v.id, v.title as title, v.detail, s.speaker_name
        from video v
        inner join speaker s
        on v.speaker_id = s.id
        inner join course c
        on v.course_id = c.id
        <where>
            <if test="title != null">
                and title like concat('%',#{title},'%')
            </if>
            <if test="speaker_id != null">
                and v.speaker_id = #{speaker_id}
            </if>
            <if test="course_id != null">
                and v.course_id = #{course_id}
            </if>
        </where>
    </select>
</mapper>